{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=vibecheck;Username=postgres;Password=bekzod28072009"
  },
  "Jwt": {
    "Issuer": "VibeCheckAPI",
    "Audience": "VibeCheckClient",
    "_KeyNote": "Dev placeholder only. In production, set this via an environment variable or user-secrets — never commit a real signing key.",
    "Key": "dev-only-placeholder-key-replace-me-1234567890",
    "AccessTokenExpirationMinutes": 30,
    "RefreshTokenExpirationDays": 30
  },
  "Cors": {
    "AllowedOrigins": []
  },
  "SuperAdmin": {
    "_Note": "Seeded once at startup if no SuperAdmin exists yet — this is the ONLY way a SuperAdmin account is ever created, there is no API for it. Change these before your first real run, then move them to user-secrets/an environment variable and remove them from this committed file.",
    "Email": "owner@vibecheck.dev",
    "Password": "ChangeMe123!"
  }
}
